# Survey Platform

A full-stack survey app built with:
- React (frontend)
- Node.js + Express (backend)
- MongoDB (database)

## Run backend
npm install
npm run server

## Run frontend
cd client
npm install
npm run dev
